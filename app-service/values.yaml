template-node-react:
  replicaCount: 2
  image:
    repository: us.icr.io/catalyst-team/template-node-typescript
    tag: 1.0.0-1
    secretName: us-icr-io
    pullPolicy: IfNotPresent
    port: 3000
  ingress:
    enabled: true
    annotations: {}
      # kubernetes.io/ingress.class: nginx
      # kubernetes.io/tls-acme: "true"
    hosts:
      - host: hello
        path: "/"
